import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    viewModal: {
        marginTop: 350,
        backgroundColor: 'white',
        padding: 20,
        borderRadius: 20,
        margin: 20,
        backgroundColor: 'red'
    },
  });